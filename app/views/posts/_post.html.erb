  <!-- 
  <% if post.imagen? %>
      <div class="post-picture-wrapper">
        <%= image_tag post.imagen.url(:thumb) %>

      </div>
      <% end %> -->



      <div class="post_panel">
        <%= avatar_for(post.user, size:40) %>
        <%= link_to post.user.name, post.user %>
        <div class="main-body">
  <h3 class="post-title"><%= link_to post.title, post %></h3>
        </div>




         <p><%=raw tag_links(post.all_tags)%> </p>
       </div>